# Monitoring mikrotik and cisco in network

first copy .env.example to .env and assign real values

## local test

### install ngrok

for example in linux `wget https://bin.equinox.io/c/4VmDzA7iaHb/ngrok-stable-linux-amd64.zipunzip ngrok-stable-linux-amd64.zip`

